# Themes

A collection of my Visual Studio Code themes.

## Use

Run `bun run build && bun run deploy` to build and install the themes.

Alternatively, you can install it manually after building:

1. `File > Preferences > Extensions > Install from VSIX...`
2. `code --install-extension themes.vsix`